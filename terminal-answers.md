# Task 5.3 - Terminal Command Answers

## 1. How many HTML files are in the project?

4

Command used:

find . -name "*.html" | wc -l

---

## 2. Which files contain the word "contact"?

(Paste the output from grep here.)

Command used:

grep -ri "contact" .

---

## 3. How many lines are in the CSS file?

(Paste the number from wc -l.)

Command used:

wc -l css/styles.css

---

## 4. Last 10 terminal commands

(Paste the output from history | tail -10.)

Command used:

history | tail -10
